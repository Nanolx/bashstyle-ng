1. lscd/tree: when mkdir is disabled and the desired directory doesn't exit, no error is thrown
2. when a command fails the next command will show a failure aswell in the prompt if that feature is enabled
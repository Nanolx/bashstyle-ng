@c -*- texinfo -*-

@chapter Aliases

Aliases allow a string to be substituted for a word when it is used as the first
word of a simple command.

For example an alias of @code{myls=ls --colors=auto --directories-first}
will issue @code{ls --colors=auto --directories-first} every time you issue @code{myls}.

In the @b{BashStyle-NG} UI you can define up to nine aliases in @code{name=prog} form.

In @file{~/.bashrc} or @file{~/.bashstyle.custom} you can define as many as desired in
@code{alias name=prog} form.

@float Figure,fig:screenshot_aliases
@center @image{screenshots/bashstyle-ng_aliases,,, BashStyle-NG Aliases Settings, png}
@caption{Aliases Settings}
@end float
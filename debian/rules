#!/usr/bin/make -f

export DH_VERBOSE = 1

%:
	dh $@

override_dh_install:
	dh_testdir
	dh_testroot
	dh_clean -k
	dh_installdirs
	DESTDIR=$(CURDIR)/debian/bashstyle-ng/ DISABLE_POSTINSTALL=1 ./make install
